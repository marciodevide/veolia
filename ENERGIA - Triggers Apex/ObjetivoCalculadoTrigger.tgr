trigger ObjetivoCalculadoTrigger on Objetivo_Calculado__c (after delete, after insert, after undelete, 
after update, before delete, before insert, before update) {
	TriggerFactory.createHandler(Objetivo_Calculado__c.sObjectType);
}