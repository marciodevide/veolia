trigger ObjetivoTrigger on Objetivo__c (after delete, after insert, after undelete, 
after update, before delete, before insert, before update) {

	TriggerFactory.createHandler(Objetivo__c.sObjectType);
}